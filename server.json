{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.meetstream-ai/meetstream-mcp",
  "title": "MeetStream",
  "description": "Meeting bots for Zoom, Google Meet and Teams: record, transcribe, summarize, run voice agents.",
  "version": "0.3.2",
  "websiteUrl": "https://docs.meetstream.ai/build-with-ai/meetstream-mcp-server",
  "repository": {
    "url": "https://github.com/meetstream-ai/meetstream-mcp",
    "source": "github"
  },
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://mcp.meetstream.ai/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "Your MeetStream API key as a bearer token, in the form 'Bearer ms_...'. Create a key at https://app.meetstream.ai/api-key. The hosted server is multi-tenant and stores no key of its own; each request carries its own.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        }
      ]
    }
  ]
}
